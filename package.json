{
  "name": "over-the-edge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:game": "vite --config vite.game.config.ts",
    "dev:art": "wrangler dev --config wrangler.local.toml --ip 127.0.0.1 --port 8787",
    "check:worker": "tsc --noEmit -p tsconfig.worker.json",
    "build": "tsc --noEmit && tsc --noEmit -p tsconfig.worker.json && vite build",
    "build:game": "tsc --noEmit -p tsconfig.game.json && vite build --config vite.game.config.ts",
    "preview": "vite preview",
    "preview:game": "vite preview --config vite.game.config.ts",
    "verify": "npm run build && node scripts/verify-runtime.mjs && npm run verify:game",
    "verify:game": "npm run build:game && node scripts/verify-game.mjs",
    "verify:art": "npm run build && node scripts/verify-art.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260925.1",
    "@types/three": "0.185.4",
    "playwright": "1.63.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "wrangler": "4.140.0"
  },
  "dependencies": {
    "jose": "6.2.12",
    "planck": "1.4.2",
    "three": "0.185.1"
  },
  "description": "A browser-based physics climbing playground inspired by Getting Over It, built with Three.js and Planck.js.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/poshih/over-the-edge.git"
  }
}
